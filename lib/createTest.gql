mutation {
    createTest(
        testType: { name: "Cucumber" },
        gherkin: "$gherkin",
        jira: {
            fields: { summary:"$title", project: {key: "MCF"} }
        }
    ) {
        test {
            jira(fields: ["key"])
        }
    }
}
